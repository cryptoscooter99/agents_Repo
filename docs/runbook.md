# Runbook

## Local development
```bash
# install deps if using Node tools
# npm ci

# run checks (customize in scripts/)
./scripts/check.sh
```

## Consent tokens
Toggle capabilities in `src/config/agent.config.json` before each session.
