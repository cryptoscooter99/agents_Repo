# Logging & Artifacts

- Write logs to `logs/` (timestamps + command output).
- Save test results, screenshots, and diffs to `artifacts/`.
- Summaries should include: task, plan, diff, commands, results, and next steps.
